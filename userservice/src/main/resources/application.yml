spring:
  application:
    name: userservice

  datasource:
    url: jdbc:mysql://localhost:3306/fitness-user
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
    port: 8081
